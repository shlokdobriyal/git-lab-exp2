name: CI for git-lab-exp2

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show repo files
        run: ls -la

      - name: Print success message
        run: echo "CI Pipeline running for shlokdobriyal/git-lab-exp2 ✅"
